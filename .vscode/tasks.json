{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Lambda Layerのビルド",
      "type": "shell",
      "command": "./script/aws-Layer-build.sh",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "reveal": "always",
        "panel": "new"
      },
      "problemMatcher": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PATH": "${workspaceFolder}/.venv/bin:${env:PATH}"
        }
      }
    }
  ]
}